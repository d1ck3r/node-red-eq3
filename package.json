{
    "name": "node-red-eq3",
    "description": "node-red flow(s) for eqiva eq-3 to be used in combination with balena-node-red-eq3 repository",
    "version": "0.0.1",
    "dependencies": {},
    "node-red": {
        "settings": {
            "flowFile": "flows.json",
            "credentialsFile": "flows_cred.json"
        }
    }
}